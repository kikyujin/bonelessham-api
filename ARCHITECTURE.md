# HAMLOG HTTP API - Architecture

## 📐 システムアーキテクチャ

```
┌──────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Browser    │  │  Python App  │  │  JavaScript  │      │
│  │   (UI.html)  │  │   (test.py)  │  │   (Custom)   │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                            │                                 │
│                            │ HTTP/REST                       │
│                            ↓                                 │
└────────────────────────────┼─────────────────────────────────┘
                             │
┌────────────────────────────┼─────────────────────────────────┐
│                      API SERVER LAYER                        │
├────────────────────────────┼─────────────────────────────────┤
│                            │                                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │         Flask HTTP Server (hamlog_api_server.py)       │ │
│  │                                                          │ │
│  │  ┌─────────────────────────────────────────────────┐   │ │
│  │  │  Endpoints:                                      │   │ │
│  │  │  - GET  /api/status                              │   │ │
│  │  │  - GET  /api/callsign/{callsign}                 │   │ │
│  │  │  - POST /api/log                                 │   │ │
│  │  │  - PUT  /api/log                                 │   │ │
│  │  │  - POST /api/clear                               │   │ │
│  │  └─────────────────────────────────────────────────┘   │ │
│  │                            │                            │ │
│  │                            │ subprocess.run()           │ │
│  │                            ↓                            │ │
│  └────────────────────────────┼────────────────────────────┘ │
│                               │                              │
│                               │ JSON stdin/stdout            │
│                               ↓                              │
└───────────────────────────────┼──────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────┐
│                   AUTOMATION LAYER                           │
├───────────────────────────────┼──────────────────────────────┤
│                               │                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │     AutoHotkey Script (hamlog_api.ahk)                 │ │
│  │                                                          │ │
│  │  ┌────────────────────────────────────────────────┐    │ │
│  │  │  Functions:                                     │    │ │
│  │  │  - CheckStatus()                                │    │ │
│  │  │  - GetCallsignData(callsign)                    │    │ │
│  │  │  - CreateLog(jsonData)                          │    │ │
│  │  │  - UpdateLog(jsonData)                          │    │ │
│  │  │  - ClearLog()                                   │    │ │
│  │  └────────────────────────────────────────────────┘    │ │
│  │                            │                            │ │
│  │                            │ Win32 API                  │ │
│  │                            │ ControlSetText             │ │
│  │                            │ ControlGetText             │ │
│  │                            │ ControlSend                │ │
│  │                            ↓                            │ │
│  └────────────────────────────┼────────────────────────────┘ │
│                               │                              │
└───────────────────────────────┼──────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────┐
│                     TARGET APPLICATION                       │
├───────────────────────────────┼──────────────────────────────┤
│                               │                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           Turbo HAMLOG (hamlogw.exe)                   │ │
│  │                                                          │ │
│  │  ┌────────────────────────────────────────────────┐    │ │
│  │  │  LOGダイアログ (TForm_A)                       │    │ │
│  │  │                                                  │    │ │
│  │  │  TEdit14: Callsign                              │    │ │
│  │  │  TEdit13: Date                                  │    │ │
│  │  │  TEdit12: Time                                  │    │ │
│  │  │  TEdit11: His RST                               │    │ │
│  │  │  TEdit10: My RST                                │    │ │
│  │  │  TEdit9:  Freq                                  │    │ │
│  │  │  TEdit8:  Mode                                  │    │ │
│  │  │  TEdit7:  Code                                  │    │ │
│  │  │  TEdit6:  G/L                                   │    │ │
│  │  │  TEdit5:  QSL                                   │    │ │
│  │  │  TEdit4:  Name                                  │    │ │
│  │  │  TEdit3:  QTH                                   │    │ │
│  │  │  TEdit2:  Rem1                                  │    │ │
│  │  │  TEdit1:  Rem2                                  │    │ │
│  │  └────────────────────────────────────────────────┘    │ │
│  │                            │                            │ │
│  │                            ↓                            │ │
│  │                    Binary Database                      │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 データフロー

### 1. コールサイン検索 (GET /api/callsign/{callsign})

```
[Client]
   │
   │ GET /api/callsign/JA1ABC
   ↓
[Flask Server]
   │
   │ subprocess: AutoHotkey.exe hamlog_api.ahk GetCallsignData JA1ABC
   ↓
[AHK Script]
   │
   │ 1. GetHamlogWindow() → TForm_Aを検索
   │ 2. ControlSetText(TEdit14, "JA1ABC")
   │ 3. ControlSend(TEdit14, {Enter})
   │ 4. Sleep(300)
   │ 5. ControlGetText(各TEdit) → データ取得
   │ 6. OutputJSON() → JSON出力
   ↓
[HAMLOG]
   │
   │ コールサイン入力 → DB検索 → フィールド更新
   ↓
[AHK Script]
   │
   │ JSON: {"callsign":"JA1ABC", "name":"山田太郎", ...}
   ↓
[Flask Server]
   │
   │ JSON parse → HTTP Response
   ↓
[Client]
   │
   │ {"callsign":"JA1ABC", "name":"山田太郎", ...}
```

### 2. ログ作成 (POST /api/log)

```
[Client]
   │
   │ POST /api/log
   │ Body: {"callsign":"JH1XYZ", "his":"59", ...}
   ↓
[Flask Server]
   │
   │ subprocess: AutoHotkey.exe hamlog_api.ahk CreateLog '{...}'
   ↓
[AHK Script]
   │
   │ 1. ParseJSON(jsonData)
   │ 2. GetHamlogWindow()
   │ 3. WinActivate → Alt+A (クリア)
   │ 4. SetAllFields(各TEdit) → データセット
   ↓
[HAMLOG]
   │
   │ フィールド更新 → (保存は手動)
   ↓
[AHK Script]
   │
   │ JSON: {"status":"success", ...}
   ↓
[Flask Server]
   │
   │ HTTP Response
   ↓
[Client]
   │
   │ {"status":"success", ...}
```

---

## 🏗️ ファイル構成

```
hamlog-api/
│
├── 📄 Core Files
│   ├── hamlog_api_server.py    # Flask HTTP server (2.6KB)
│   ├── hamlog_api.ahk          # AHK automation script (7.1KB)
│   └── requirements.txt        # Python dependencies (46B)
│
├── 🧪 Testing & Tools
│   ├── hamlog_api_test.py      # Test client (5.0KB)
│   └── hamlog_api_ui.html      # Web UI (9.8KB)
│
├── 🚀 Setup & Deploy
│   └── setup.bat               # Windows setup (1.8KB)
│
└── 📚 Documentation
    ├── HAMLOG_API_README.md    # Detailed docs (5.9KB)
    └── QUICKSTART.md           # Quick start guide (5.5KB)
```

**Total: 8 files, ~38KB**

---

## 🔐 セキュリティ考慮事項

### 現在の制限

- **localhost専用**: `127.0.0.1`でのみリッスン
- **認証なし**: APIキー・トークンなし
- **CORS無効**: クロスオリジン制限なし(localhost内)

### 本番環境への移行時の推奨事項

1. **認証追加**
   ```python
   from functools import wraps
   
   def require_api_key(f):
       @wraps(f)
       def decorated(*args, **kwargs):
           token = request.headers.get('X-API-Key')
           if token != 'your-secret-key':
               return jsonify({'error': 'Unauthorized'}), 401
           return f(*args, **kwargs)
       return decorated
   
   @app.route('/api/log', methods=['POST'])
   @require_api_key
   def create_log():
       # ...
   ```

2. **HTTPS化**
   - 自己署名証明書またはLet's Encrypt
   - `app.run(ssl_context='adhoc')`

3. **レート制限**
   ```python
   from flask_limiter import Limiter
   
   limiter = Limiter(app, default_limits=["100 per hour"])
   ```

4. **ログ記録**
   ```python
   import logging
   logging.basicConfig(filename='api.log', level=logging.INFO)
   ```

---

## 🎯 拡張可能性

### Phase 1 (現在)
- ✅ 基本CRUD操作
- ✅ コールサイン検索
- ✅ ログ作成/更新/クリア

### Phase 2 (計画中)
- [ ] 自動保存API
- [ ] ログ検索API(日付範囲、QTH、Mode等)
- [ ] バッチ操作(複数ログ一括処理)
- [ ] QSLカード管理API

### Phase 3 (将来)
- [ ] WebSocket対応(リアルタイム同期)
- [ ] Electron製デスクトップアプリ
- [ ] モバイルアプリ(React Native)
- [ ] クラウド同期(Firebase, AWS)

---

## 🤖 CI/CDパイプライン案

```yaml
# .github/workflows/test.yml
name: Test HAMLOG API

on: [push, pull_request]

jobs:
  test:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: pip install -r requirements.txt
    
    - name: Run tests
      run: python hamlog_api_test.py all
```

---

**73! de JS2OIA / Kikyujin**
